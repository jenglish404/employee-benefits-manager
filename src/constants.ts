/** The base cost of employee health benefits per year. */
export const EMPLOYEE_BENEFIT_COST_PER_YEAR = 1000;

/** The base cost of dependent health benefits per year. */
export const DEPENDENT_BENEFIT_COST_PER_YEAR = 500;

/**
 * The amount each employee is paid per pay period. For this exercise, this
 * value is a constant.
 */
export const EMPLOYEE_SALARY_PER_PAY_PERIOD = 2000;

/**
 * Limit for dependents. If it goes beyond this, we'd need to rethink
 * the UI design.
 */
export const DEPENDENT_LIMIT = 10;
